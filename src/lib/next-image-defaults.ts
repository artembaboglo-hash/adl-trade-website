/**
 * Quality for `next/image` when the built-in optimizer runs (default Next value is 75).
 * Higher values preserve detail on photos and logos at the cost of larger generated assets.
 */
export const NEXT_PHOTO_IMAGE_QUALITY = 92 as const;
