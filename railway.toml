# https://docs.railway.com/reference/config-as-code
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Matches Dockerfile CMD; clear any custom override in the Railway service settings if it conflicts.
startCommand = "node .next/standalone/server.js"
